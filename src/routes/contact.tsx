import { createFileRoute } from "@tanstack/react-router"; import { useState, type FormEvent } from "react"; import { Bug, CheckCircle2, Mail, MessageSquareText, Send } from "lucide-react"; import { SiteLayout } from "@/components/study-sync/site-shell"; import { PageHero } from "@/components/study-sync/page"; import { Button } from "@/components/ui/button"; import { Input } from "@/components/ui/input"; import { Label } from "@/components/ui/label"; import { Textarea } from "@/components/ui/textarea";
export const Route=createFileRoute("/contact")({head:()=>({meta:[{title:"Contact — Study Sync"},{name:"description",content:"Contact Study Sync, share product feedback or report a bug."},{property:"og:title",content:"Contact Study Sync"},{property:"og:description",content:"Questions, feedback and product support."},{property:"og:type",content:"website"},{name:"twitter:card",content:"summary_large_image"}]}),component:Contact});
function Contact(){const [mode,setMode]=useState("contact");const [sent,setSent]=useState(false);function submit(e:FormEvent){e.preventDefault();setSent(true)}return <SiteLayout><PageHero eyebrow="Talk to our team" title="Questions, ideas or something not working?" copy="Choose the kind of message you want to send. We’ll make sure it reaches the right place."/><section className="px-5 pb-24 sm:px-8"><div className="mx-auto grid max-w-5xl gap-6 lg:grid-cols-[.7fr_1.3fr]"><div className="space-y-3">{[["contact",Mail,"Contact"],["feedback",MessageSquareText,"Feedback"],["bug",Bug,"Report a bug"]].map(([m,I,l])=>{const Icon=I as typeof Mail;return <Button key={String(m)} variant={mode===m?"default":"outline"} className="h-14 w-full justify-start" onClick={()=>{setMode(String(m));setSent(false)}}><Icon/>{String(l)}</Button>})}<div className="glass-soft rounded-lg p-5 text-sm leading-6 text-muted-foreground"><p className="font-medium text-foreground">Response time</p><p className="mt-1">We usually reply within two business days.</p></div></div><div className="glass-panel rounded-lg p-7">{sent?<div className="grid min-h-80 place-items-center text-center"><div><CheckCircle2 className="mx-auto size-10 text-primary"/><h2 className="mt-5 text-2xl font-semibold">Message received</h2><p className="mt-2 text-sm text-muted-foreground">Thanks for helping us make Study Sync better.</p><Button className="mt-6" variant="outline" onClick={()=>setSent(false)}>Send another</Button></div></div>:<form onSubmit={submit} className="space-y-5"><div><h2 className="text-2xl font-semibold">{mode==="contact"?"Contact Study Sync":mode==="feedback"?"Share feedback":"Report a bug"}</h2><p className="mt-2 text-sm text-muted-foreground">Tell us what’s on your mind.</p></div><div className="grid gap-4 sm:grid-cols-2"><Field label="Name" name="name"/><Field label="Email" name="email" type="email"/></div><Field label={mode==="bug"?"Where did it happen?":"Subject"} name="subject"/><div className="space-y-2"><Label htmlFor="message">Message</Label><Textarea id="message" required className="min-h-40" placeholder={mode==="bug"?"What happened, and what did you expect?":"How can we help?"}/></div><Button><Send/>Send message</Button></form>}</div></div></section></SiteLayout>}
function Field({label,name,type="text"}:{label:string;name:string;type?:string}){return <div className="space-y-2"><Label htmlFor={name}>{label}</Label><Input id={name} name={name} type={type} required className="h-11"/></div>}