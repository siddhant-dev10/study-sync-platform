import { createFileRoute, Link } from "@tanstack/react-router";
import { useState, type FormEvent } from "react";
import { ArrowLeft, Mail } from "lucide-react";
import { supabase } from "@/integrations/supabase/client";
import { Button } from "@/components/ui/button"; import { Input } from "@/components/ui/input"; import { Label } from "@/components/ui/label"; import { Logo } from "@/components/study-sync/logo";
export const Route=createFileRoute("/forgot-password")({head:()=>({meta:[{title:"Reset Password — Study Sync"},{name:"description",content:"Request a secure Study Sync password reset."},{property:"og:title",content:"Reset Password — Study Sync"},{property:"og:description",content:"Recover your Study Sync account."},{property:"og:type",content:"website"},{name:"twitter:card",content:"summary"}]}),component:Forgot});
function Forgot(){const [msg,setMsg]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const email=String(new FormData(e.currentTarget).get("email"));const {error}=await supabase.auth.resetPasswordForEmail(email,{redirectTo:window.location.origin+"/reset-password"});setMsg(error?.message??"Check your email for a secure password reset link.")}return <AuthShell title="Reset your password" copy="We’ll send a secure recovery link to your inbox."><form onSubmit={submit} className="space-y-4"><Label htmlFor="email">Email</Label><Input id="email" name="email" type="email" required className="h-11"/>{msg&&<p className="text-sm text-muted-foreground">{msg}</p>}<Button className="h-11 w-full"><Mail/>Send reset link</Button><Button variant="ghost" className="w-full" asChild><Link to="/login"><ArrowLeft/>Back to login</Link></Button></form></AuthShell>}
export function AuthShell({title,copy,children}:{title:string;copy:string;children:React.ReactNode}){return <div className="flex min-h-screen items-center justify-center px-5 py-24"><div className="w-full max-w-md"><div className="mb-8 text-center"><Logo/><h1 className="mt-8 text-3xl font-semibold">{title}</h1><p className="mt-2 text-sm text-muted-foreground">{copy}</p></div><div className="glass-panel rounded-lg p-7">{children}</div></div></div>}