import { createFileRoute, useNavigate } from "@tanstack/react-router";
import { useEffect, useState, type FormEvent } from "react";
import { KeyRound } from "lucide-react";
import { supabase } from "@/integrations/supabase/client"; import { Button } from "@/components/ui/button"; import { Input } from "@/components/ui/input"; import { Label } from "@/components/ui/label"; import { AuthShell } from "./forgot-password";
export const Route=createFileRoute("/reset-password")({head:()=>({meta:[{title:"Choose New Password — Study Sync"},{name:"description",content:"Set a new password for your Study Sync account."},{property:"og:title",content:"Choose New Password — Study Sync"},{property:"og:description",content:"Securely update your Study Sync password."},{property:"og:type",content:"website"},{name:"twitter:card",content:"summary"}]}),component:Reset});
function Reset(){const nav=useNavigate();const [valid,setValid]=useState(false);const [msg,setMsg]=useState("Checking your recovery link…");useEffect(()=>{const recovery=window.location.hash.includes("type=recovery")||new URLSearchParams(window.location.search).get("type")==="recovery";supabase.auth.getSession().then(({data})=>{setValid(recovery||Boolean(data.session));setMsg(recovery||data.session?"":"This recovery link is invalid or expired.")})},[]);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const pw=String(new FormData(e.currentTarget).get("password"));const {error}=await supabase.auth.updateUser({password:pw});if(error)setMsg(error.message);else await nav({to:"/profile"})}return <AuthShell title="Choose a new password" copy="Use at least eight characters.">{!valid?<p className="text-sm text-muted-foreground">{msg}</p>:<form onSubmit={submit} className="space-y-4"><Label htmlFor="password">New password</Label><Input id="password" name="password" type="password" minLength={8} required className="h-11"/>{msg&&<p className="text-sm text-muted-foreground">{msg}</p>}<Button className="h-11 w-full"><KeyRound/>Update password</Button></form>}</AuthShell>}